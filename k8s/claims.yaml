apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: assignment-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi 
  storageClassName: "assignment-storage-class"